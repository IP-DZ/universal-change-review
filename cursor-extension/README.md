# Universal Change Review for Cursor

Click **Changes Review** in Cursor's bottom status bar, or use the **Changes Review** activity-bar icon, to open the current workspace's live, read-only Git Changes panel in an editor tab.

The extension binds its local panel only to `127.0.0.1`, uses a random access token, and exposes no write or revert operation.
